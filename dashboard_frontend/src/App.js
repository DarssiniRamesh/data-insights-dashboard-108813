import React from 'react';
import './styles/tokens.css';
import VotingDashboard from './pages/VotingDashboard';

/**
 * PUBLIC_INTERFACE
 * App
 * Entrypoint rendering the app-voter dashboard layout shell (Sidebar, Topbar, responsive grid).
 * Widgets are placeholders at this stage.
 */
function App() {
  return <VotingDashboard />;
}

export default App;
